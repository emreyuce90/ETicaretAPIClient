import { ProductListWCount } from './product-list-wcount';

describe('ProductListWCount', () => {
  it('should create an instance', () => {
    expect(new ProductListWCount()).toBeTruthy();
  });
});
