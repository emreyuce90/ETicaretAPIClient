import { CustomerListWCount } from './customer-list-wcount';

describe('CustomerListWCount', () => {
  it('should create an instance', () => {
    expect(new CustomerListWCount()).toBeTruthy();
  });
});
